# Шаг 9 — Правка (ответ по находкам шага 7)

Шаг 7 дал две правдоподобные находки, ноль подтверждённых дефектов этой правки.

- **Находка А** (риск, если `dealBriefSentences` когда-нибудь вызовут не для сделки) —
  **возражение**: сегодня функция вызывается только для сделок (`D().deals.find(...)` перед
  вызовом, `dealBrief`/`dealStatusBrief`); расширение защиты `conflictSource` под не-сделки
  выходит за объявленную границу правки (`.takt/evidence/cold-operator/00-granica.txt`
  называет только `src/js/ui.js` и `src/test/smoke.js`, а намерение — только адрес поиска
  конфликта). Не игнорируется молча: находка названа здесь и остаётся открытой на будущее,
  а не спрятана.
- **Находка Б** (порядок приоритета причин не пересмотрен) — **возражение**: намерение
  (шаг 0, «Что не входит») явно исключило пересмотр приоритета из объёма этой правки.

Правка кода не требуется: диффа с шага 4 не меняю.
