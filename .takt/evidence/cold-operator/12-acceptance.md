# Шаг 12 — Приёмка против намерения

Отвечаю по каждому пункту `00-intent.md`.

## «Что входит»

1. **Подтвердить/опровергнуть, что `cf` ищется неверно.** Подтверждено чтением:
   `src/js/fixtures.js:1070-1072` — `conflicts` ключ `r_karim`; `src/js/fixtures.js:444` —
   `d_karim_cross.requestId = 'r_karim_cross'`, а `d_karim_cross.id = 'd_karim_cross'` —
   разные строки, прямой lookup `[d.id]` бьёт мимо. Механическое подтверждение того же факта
   несёт шаг 3, отдельным прогоном: `node src/test/smoke.js` (rc=1 до правки,
   `out/03-acceptance-red.txt`).
2. **Заменить прямой поиск на `conflictSource(d)`.** Сделано, `src/js/ui.js:7659-7666`.
   Размер диффа не рос после шага 4: `git diff --stat -- src/js/ui.js` (см. вывод ниже).

       1 file changed, 6 insertions(+), 1 deletion(-)

3. **Сторожевой тест в `smoke.js`, подтверждённый мутацией.** `node src/test/smoke.js`

       checks: 2174  passed: 2174  FAILED: 0

   (`out/10-mutation-green.txt`), после того как мутация на шаге 10 дала ровно один красный
   с тем же именем теста (`out/10-mutation-red.txt`).

## «Что не входит» — соблюдено

Порядок приоритета причин не тронут; `conflictBlock` не тронут; пункты 1–4
`dealBriefSentences` не тронуты — весь дифф начинается с комментария «5. Что мешает».

## «Чем меряем» — критерий из намерения выполнен

До правки `node src/test/smoke.js` (`out/03-acceptance-red.txt`) — ровно один красный,
дословная причина: `строка_Мешает="Мешает: не закрыт шаг «Подтверждение партнёра»."`
(«Бюджет» отсутствует). После правки `node src/test/smoke.js` (`out/10-mutation-green.txt`):

    checks: 2174  passed: 2174  FAILED: 0

Новых красных относительно прогона до появления теста: 0.

## Граница

`git diff --name-only` от начального коммита + неотслеживаемые совпадает построчно с
`.takt/evidence/cold-operator/00-granica.txt`: `src/js/ui.js`, `src/test/smoke.js`.
Нарушений границы такт не зафиксировал ни на одном шаге.

## Честно неисполненное

Шаги 7 и 10 не декоррелированы: тот же оператор, что вёл 4 и 9 — объявлено в `00-intent.md`
и в отчётах 07/10. Такт этого не запрещает механически (`same_actor` сравнивает буквальные
строки `actor`, а не физическое лицо), только фиксирует в журнале то, что там записано.
Приёмка называет это слабостью доказательства, а не молчит о ней.
