Механическая сверка Task 8. Категория tests, состав объявлен в .takt/commands.json.
Команда, как она объявлена, дословно:

    npm test && npm run test:proxy

Ниже — вывод каждой её половины, снятый отдельным вызовом, чтобы код возврата
каждой был виден сам по себе, а не поглощён цепочкой.
Категории lint и types в этом репозитории не заведены — третий исход.

======================================================================

$ npm test
  ✓ все экраны рисуются на старом снимке, в обеих ролях
  ✓ запросы брокера теперь переживают перезагрузку

Итог: 8 прошло, 0 провалилось
отправок после первой попытки: 1
  причина      : probe
  подпись      : 670236234.5024
  запросы      : ["как продавать Creekline 1208","кому подходит этот объект"]
  вопрос в треде: ЕСТЬ
  размер       : 5 КБ
отправок после второй попытки (ничего не менялось): 1
отправок после изменения: 2

ИТОГ: отправляет по изменению, не спамит
NPM_TEST_RC=0

$ npm run test:proxy
  OK  a call that keeps streaming is left alone though it runs well past that window

all proxy checks passed
TEST_PROXY_RC=0
