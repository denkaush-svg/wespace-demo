checks: 2071  passed: 2053  FAILED: 18

--- failures ---
  ✗ диалоги · кнопка запуска списка диалогов называется «Выбрать диалог», а не «Новый» (приёмка: переименовать кнопку)  [текст кнопки: «Новый»]
  ✗ диалоги · окно выбора называется «Выбрать диалог», а не «Новый диалог»  [заголовок: «Новый диалог»]
  ✗ диалоги · окно честно говорит, что выбор из списка открывает СУЩЕСТВУЮЩИЙ диалог, а не заводит новый (приёмка: подписать в списках)  [слова «существ...» рядом со списком нет: Новый диалогО чём разговор? Выберите сделку, заявку или контакт — разговор привяжется к ним и будет виден из карточки.СделкиАнна Петрова · Подготовка к сделкеВиктор Орлов · Бронь (EOI)Виктор Орлов · Подготовка к сделкеSa]
  ✗ диалоги · есть отдельное действие «Новый общий диалог» (не то же самое, что переиспользуемый «Общий диалог»)  [кнопка с текстом «Новый общий диалог» нигде на экране не найдена]
  ✗ диалоги · «Новый общий диалог» открывает пустой тред (приёмка: дословно из плана)  [кнопки «Новый общий диалог» нет — сценарий не воспроизводим]
  ✗ диалоги · тред сделки Анны нельзя очистить действием «новый диалог»  [кнопки «Новый общий диалог» нет — сценарий не воспроизводим]
  ✗ карточка сделки · рядом с «Написать» сказано, что откроется существующий диалог, а не новый (приёмка: подписать в списках)  [ПозвонитьНаписать]
  ✗ список объектов · рядом с «Чат по объекту» сказано про существующий диалог (приёмка: подписать в списках)  [ДоходностьОценитьПродвижениеВ подборкуДокументыЧат по объекту]
  ✗ карточка заявки · рядом с «Написать клиенту» сказано про существующий диалог (приёмка: подписать в списках)  [Добавить объектСобрать оффер клиентуНаписать клиентуИзменить запросОткрыть контакт]
  ✗ глубина · описание активной глубины «fast» видно как текст рядом с переключателем, а не только по наведению (приёмка: 390px без наведения)  [ПрикрепитьАвто▾Контекст▾БыстроРазмышлениеГлубокоДиктовка недоступна в этом браузереНабрать текстом]
  ✗ глубина · описание активной глубины «think» видно как текст рядом с переключателем, а не только по наведению (приёмка: 390px без наведения)  [ПрикрепитьАвто▾Контекст▾БыстроРазмышлениеГлубокоДиктовка недоступна в этом браузереНабрать текстом]
  ✗ глубина · описание активной глубины «deep» видно как текст рядом с переключателем, а не только по наведению (приёмка: 390px без наведения)  [ПрикрепитьАвто▾Контекст▾БыстроРазмышлениеГлубокоДиктовка недоступна в этом браузереНабрать текстом]
  ✗ глубина · aria-pressed кнопки «fast» отражает, активна ли она (приёмка: aria-pressed кнопкам режимов)  [aria-pressed=null (ожидали «false»)]
  ✗ глубина · кнопка «fast» несёт aria-describedby на реально существующий элемент с описанием (приёмка: aria-describedby кнопкам режимов)  [атрибута aria-describedby нет]
  ✗ глубина · aria-pressed кнопки «think» отражает, активна ли она (приёмка: aria-pressed кнопкам режимов)  [aria-pressed=null (ожидали «true»)]
  ✗ глубина · кнопка «think» несёт aria-describedby на реально существующий элемент с описанием (приёмка: aria-describedby кнопкам режимов)  [атрибута aria-describedby нет]
  ✗ глубина · aria-pressed кнопки «deep» отражает, активна ли она (приёмка: aria-pressed кнопкам режимов)  [aria-pressed=null (ожидали «false»)]
  ✗ глубина · кнопка «deep» несёт aria-describedby на реально существующий элемент с описанием (приёмка: aria-describedby кнопкам режимов)  [атрибута aria-describedby нет]
RC=1
