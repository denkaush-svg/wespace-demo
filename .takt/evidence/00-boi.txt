# Боевой признак. `команда` ⇒ `ожидаемая подстрока`. Строка «не раньше» — нижняя
# граница даты проверки.
#
# Смысл: закрыть разрыв, названный прошлым прогоном (SCORE.md 2026-09-02, «оснастка
# такта», пункт 1) — между локальным слиянием (шаг 13) и проверкой в бою (шаг 16)
# не было ни одного шага, и путь между ними держался на памяти человека. Здесь оба
# признака бьют НЕ по временной рабочей копии (`.worktrees/...`), а по настоящему
# репозиторию `wespace-demo` на его собственной ветке (`fix/broker-flow-integrity`) —
# то есть по тому дереву, из которого реально собирается `index.html`, публикуемый
# GitHub Pages. Токен SHA ниже разрешается автоматически из свидетельства шага 13.

`git -C C:/Users/Lenovo/Documents/wespace-demo log -1 --format=%H` ⇒ `SHA`
`cd C:/Users/Lenovo/Documents/wespace-demo && npm test 2>&1 | tail -1` ⇒ `passed`

не раньше: 2026-09-04
